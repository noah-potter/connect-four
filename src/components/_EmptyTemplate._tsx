import React from 'react'
import styled from 'styled-components'

const Root = styled.div``

type Props = {}

export const EmptyFunction: React.FC<Props> = (props) => {
  return (
    //
    <Root />
  )
}

type ContainerProps = {}

export const EmptyContainer: React.FC<ContainerProps> = (props) => {
  return <EmptyFunction />
}

export default EmptyContainer